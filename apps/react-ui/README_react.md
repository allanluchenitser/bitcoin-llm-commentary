React UI front-end for the BLC monorepo.
Receives REST and SSE data from the BLC web-api.

# scaffold a typescript reactjs workspace via root (not apps/react-ui)
npm create vite@latest apps/react-ui -- --template react-ts

# run via workspaces root
npm run -w @blc/react-ui dev

